# Pregnancy Tracker (Web)

A standalone browser recreation of the Pregnancy Action Tracker's Dashboard, History, Actions, and Settings screens, built with Vite + React + TypeScript. Data is stored locally in the browser via `localStorage` — nothing is synced with the Expo/React Native app in this repo.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```
